import pygame
import random

naytto = pygame.display.set_mode((640, 400))
pygame.display.set_caption("Piirtäminen")

###################################################
#####TEHT 1########################################
###################################################

#def main():
 #   while True:
  #      tapahtuma = pygame.event.poll()
   #     if tapahtuma.type == pygame.QUIT:
    #        break

     #   naytto.fill((0, 0, 0))
      #  kuva = pygame.image.load("Krisse.png").convert()
       # naytto.blit(kuva, (200,200))
        #pygame.display.flip()

#main()

###################################################
#####TEHT 2########################################
###################################################

#kuva = pygame.image.load("Krisse.png").convert()
#def piirräkuva(kuva, x, y):
    #while True:
      #tapahtuma = pygame.event.poll()
      #if tapahtuma.type == pygame.QUIT:
            #break

      #naytto.fill((0, 0, 0))
      #naytto.blit(kuva, (x,y))
      #pygame.display.flip()


#piirräkuva(kuva, 200, 200)

###################################################
#####TEHT 3########################################
###################################################

#kuva = pygame.image.load("Krisse.png").convert()
#def piirräkuva(kuva):
    #while True:
      #tapahtuma = pygame.event.poll()
      #if tapahtuma.type == pygame.QUIT:
            #break
      #x = random.randint(0,640)
      #y = random.randint(0,400)
      #naytto.fill((0, 0, 0))
      #naytto.blit(kuva, (x,y))
      #pygame.display.flip()


#piirräkuva(kuva)
